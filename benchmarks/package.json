{
  "name": "@ereo/benchmarks",
  "version": "0.2.27",
  "private": true,
  "type": "module",
  "scripts": {
    "setup": "bash scripts/setup.sh",
    "clean": "bash scripts/clean.sh",
    "bench": "bun run scripts/run-all.ts",
    "bench:build": "bun run scripts/run-all.ts --build",
    "bench:server": "bun run scripts/run-all.ts --server",
    "bench:client": "bun run scripts/run-all.ts --client",
    "bench:startup": "bun run scripts/run-all.ts --startup"
  },
  "dependencies": {
    "autocannon": "^7.15.0",
    "lighthouse": "^12.0.0",
    "chalk": "^5.3.0",
    "puppeteer": "^22.0.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.4.0"
  }
}
